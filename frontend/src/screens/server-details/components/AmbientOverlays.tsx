/**
 * Non-interactive detail overlays. In the light theme the dark tactical
 * vignette + corner brackets are removed for a clean flat scene; the component
 * is kept (still mounted by the detail view) but renders nothing.
 */
export function AmbientOverlays() {
  return null;
}
